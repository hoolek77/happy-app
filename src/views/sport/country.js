export default class Country {
  constructor({ country_id, name, country_code, continent }) {
    this.country_id = country_id
    this.name = name
    this.country_code = country_code
    this.continent = continent
  }
}
